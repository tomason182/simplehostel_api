export interface AddressDTO {
  houseNumber: string;
  street: string;
  city: string;
  postalCode: string;
  state: string;
  country: string;
  countryCode: string;
  lat: number;
  lon: number;
  osmId: string;
  createdAt: Date;
  updatedAt: Date;
}
